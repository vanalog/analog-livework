// TODO: Remove types once we have generated API schemas
export interface ActiveContractsStatsProps {
  activeContracts: number;
  pausedContracts: number;
  totalActiveValue: number;
  disbursedYTD: number;
}
